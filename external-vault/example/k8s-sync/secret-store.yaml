apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "http://172.17.18.150:8200"
      path: "kvv2"
      version: "v2"
      auth:
        tokenSecretRef:
          name: "vault-token-secret"
          key: "token"
